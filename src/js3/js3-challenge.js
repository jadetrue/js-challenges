/**
 * This functions takes in someones age and halves it, then adds 7.
 *
 * @param {number} age - someones age
 * @return {number} half the input age + 7
 */
export const minDateableAge = age => {
  // your code here
};

/**
 * This functions takes in a temperature in celsius and outputs if the temperature is feverous
 *
 * @param {number} tempInCelsius - temperature in celsius
 * @return {boolean} if the temperture is feverous
 */
export const hasFever = tempInCelsius => {
  // your code here
};

/**
 * Given a width on a 16:9 TV, calculates the height
 *
 * @param {number} width - TV width
 * @return {number} TV height
 */
export const calcTVHeight = width => {
  // your code here
};

/**
 * Given two peoples ages, this works out if they are the minimum dateable age for each other (deals with ages either way round)
 *
 * @param {number} age1 - A persons age
 * @param {number} age2 - A persons age
 * @return {boolean} If the people could date
 */
export const couldDate = (age1, age2) => {
  // your code here
};

/**
 * Takes in [red, green, blue] (0-255) and a darken value. This will darken the colour array by the darken amount but won't drop below 0.
 *
 * @param {array} rgbArray - An array of numbers representing red green and blue colour intensity
 * @param {number} darkenAmount - A value to darken the rgbArray by
 * @return {array} The new array of darkened colours
 */
export const colorDarken = (rgbArray, darkenAmount) => {
  // your code here
};
