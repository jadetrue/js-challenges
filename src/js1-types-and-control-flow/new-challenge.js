/**
 * Concatenate 2 strings
 * Find largest number
 * Add 2 numbers together
 *
 * Find length of string
 * Typeof something
 * Return a personalised statement based on the users age and name -> "Sorry John, you're only old enough to drink coke."
 *
 * Convert string to number "14.4" -> 14x 14.4
 * Turn a string that looks like this IAmAString to this "i_am_a_string"
 *
 * Find if a sentence contains any uppercase letters using regex, if it does, return a message a lowercase version of the message, otherwise return a message "No uppercase letter found in message"
 */
