/**
 * A function that will take two arguments: an array of colors and an index.
 * The function will test whether the item at index is not a primary color (red, yellow, blue) and if it isn't, replace it with red. The function returns the new array.
 *
 * @param {array} colorArray - An array of colours e.g. ["red", "blue"]
 * @param {number} index - Index of the array item to test
 * @return {array} A list of updated colours
 */
export const fixColors = (colorArray, index) => {
  // your code here
};
